# These values will automatically filled in at "gen" time:
# Workshopctl:
#   ClusterNumber: 97
#   Domain: workshop.kubernetesfinland.com
# You can also add your custom values below:
ingress:
  enabled: true
  paths:
  - /dashboard
  annotations:
    traefik.ingress.kubernetes.io/rewrite-target: "/"
  # The values.js pipe will fill in the following automatically:
  # hosts:
  # - cluster-{{ .Workshopctl.ClusterNumber }}.{{ .Workshopctl.Domain }}